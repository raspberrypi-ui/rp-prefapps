A customisable application to install recommended applications not included in the standard RPD image.

To build, run autogen.sh, then ./configure and make.
Use sudo make install to install data files.
